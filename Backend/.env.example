# =============================================================================
# Loan Assessment Backend - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Generate ENCRYPTION_KEY with:
#   python3 scripts/generate_encryption_key.py
# Then paste the printed ENCRYPTION_KEY=... line into .env
# =============================================================================

# ----- LLM (required for assessment) -----
# Google Gemini - used for all agents (Financial Analyst, Market Researcher, Risk Assessor, Coach)
# Get key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# ----- Plaid (required for bank connection & transaction data) -----
# Dashboard: https://dashboard.plaid.com/
# Use "Keys" → Client ID and Sandbox secret when PLAID_ENV=sandbox
PLAID_CLIENT_ID=
PLAID_SECRET=
# One of: sandbox | development | production
PLAID_ENV=sandbox

# ----- Google Maps / Places (required for market research) -----
# Console: https://console.cloud.google.com/
# Enable: Maps JavaScript API, Geocoding API, Places API (or Places API (New))
GOOGLE_MAPS_API_KEY=
GOOGLE_PLACES_API_KEY=

# ----- Security (required) -----
# Generate once: python3 scripts/generate_encryption_key.py
ENCRYPTION_KEY=

# ----- Optional -----
# DATABASE_URL=sqlite+aiosqlite:///./loan_assessment.db
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
